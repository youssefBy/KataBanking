spring:
    datasource:
        driver-class-name: org.h2.Driver
        password: sa
        url: jdbc:h2:mem:db
        username: sa
    jpa:
        hibernate:
            ddl-auto: none
        show-sql: true
    h2:
        console:
            enabled: true
            path: /h2

    profiles:
        active: test

